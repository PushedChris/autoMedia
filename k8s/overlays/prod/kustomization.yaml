apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

patches:
  - path: replicas-patch.yaml
  - path: resources-patch.yaml
  - path: api-gateway-service-patch.yaml

# 注意：镜像地址需要通过环境变量或手动修改
# 建议使用脚本生成或在 CI/CD 中设置
