# This file is managed by CI/CD pipeline
# Secrets are injected from GitHub Secrets during deployment
# Do not commit sensitive data here

apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: ai-platform
type: Opaque
data:
  # Managed by GitHub Actions CI/CD pipeline
  # MINIO_ACCESS_KEY
  # MINIO_SECRET_KEY
  # DATABASE_PASSWORD
  # REGISTRY_URL
  # REGISTRY_USERNAME
  # REGISTRY_PASSWORD
  # REGISTRY_NAMESPACE
